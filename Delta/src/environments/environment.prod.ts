export const environment = {
  production: true,
  APIEndpoint: window.location.origin,
  socket: {
    baseUrl: window.location.origin,
    config: {}
  },
  SSPBASE_PATH : 'diamond909.ssc-live.com'
};
